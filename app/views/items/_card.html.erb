<div class="card mb-3">
  <h3 class="card-header"><%= item.title %></h3>
  <img style="height: 200px; width: 100%; display: block;" src="<%= item.image_url %>" alt="Card image">
  <div class="card-body">
    <p class="card-text"><%= item.description%></p>
    <p><small class="text-muted">Prix :</small><%= item.price %> € </p>
    <a href="#" class="card-link">Lien vers le show</a>
  </div>